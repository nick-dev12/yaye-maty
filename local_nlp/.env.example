# Client NLP local — Machine Ryzen 7 (ne pas déployer sur le VPS)

# URL du VPS Django (local ou production)
YAYEMATY_API_URL=http://127.0.0.1:8000

# Même clé que sur le VPS (.env serveur)
INTELLIGENCE_API_KEY=changez-moi

# Optionnel
NLP_BATCH_LIMIT=50
NLP_TIMEOUT_SECONDS=30
